#loginbox-sign-up
  .header-claim
    = fancy_users_count
  .facebook-access-wrapper
    = render 'fancy_login/sign_up_facebook' if @config.user["has_facebook_login"]
  .google-access-wrapper
    = render 'fancy_login/sign_up_google' if @config.user["has_google_login"]

  .micro-note
    (Kelisto nunca publicará en tu muro sin tu permiso)

  %form#register_form{method: 'post', action: '#', data: { url: @config.sign_up_url }}
    .or-line o bien, con tu email
    .email.form-group
      .help-inline
      %input#register-email.form-control{autofocus: 'autofocus', name: 'user[email]', placeholder: 'Correo electrónico', size: '30', title: 'Obligatorio', type: 'text', value: ''}

    .password.form-group
      .help-inline
      %input#register-pass.form-control{name: 'user[password]', placeholder: 'Contraseña', maxlength: '30', type: 'password'}

    .other
      .help-inline

    #terms
      .help-inline
      .additional-note
        = check_box_tag "loginbox-terms-and-conditions", 0, nil, :id => "loginbox-terms-and-conditions"
        = label_tag "loginbox-terms-and-conditions" do
          Confirmo que he leído y acepto la
          %a{:href => "/politica-de-privacidad", :target => '_blank'} política de privacidad

    %button.btn.button-green.btn-large.btn-block{type: 'submit'}
      Crear cuenta

  .alternative-action.additional-note
    Si ya has creado tu cuenta,
    %br
    %a.js-switch-action{href: '#signin', rel: 'nofollow', title: "Entra en Kelisto con tu cuenta"}
      entra aquí
